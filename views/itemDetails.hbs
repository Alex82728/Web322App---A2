<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{item.name}} - Item Details</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Item Details</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/shop">Shop</a></li>
            </ul>
        </nav>
    </header>

    <div class="item-details">
        <h2>{{item.name}}</h2>
        <!-- Ensure the feature image exists before rendering -->
        {{#if item.featureImage}}
            <img src="{{item.featureImage}}" alt="{{item.name}} image" class="item-image">
        {{else}}
            <p>No image available</p>
        {{/if}}

        <p><strong>Description:</strong> {{item.description}}</p>
        <p><strong>Price:</strong> ${{item.price}}</p>
        <p><strong>Category:</strong> {{category}}</p> <!-- Display category -->

        <!-- Format postDate with the helper function if it's a valid date -->
        <p><strong>Post Date:</strong> {{#if item.postDate}}{{formatDate item.postDate}}{{else}}Not available{{/if}}</p>
    </div>

    <footer>
        <p>&copy; 2024 Motorcycle Shop. All rights reserved.</p>
    </footer>
</body>
</html>
