<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items List</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-5">
    <h2>Items List</h2>

    <table class="table">
        <thead>
            <tr>
                <th>Item Name</th>
                <th>Category</th>
                <th>Price</th>
                <th>Published</th>
                <th>Actions</th> <!-- New column for the remove button -->
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.category}}</td>
                <td>${{this.price}}</td> <!-- Add currency symbol for clarity -->
                <td>{{#if this.published}}Yes{{else}}No{{/if}}</td> <!-- Display Yes/No for published -->
                <td>
                    <!-- Remove button linking to the delete route -->
                    <form action="/items/{{this.id}}" method="POST" style="display:inline;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger">Remove</button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>

</body>
</html>
